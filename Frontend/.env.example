# Frontend (Vite) - for local dev or reference; Vercel: set in Project Settings → Environment Variables

# Backend API URL (required when frontend is on different host, e.g. Vercel)
# VITE_API_URL=https://your-api.example.com
